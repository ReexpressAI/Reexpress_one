//
//  RestoreSubscriptionButtonView.swift
//  Reexpress
//
//  Created by A on 10/9/23.
//

/*
 import SwiftUI
 import StoreKit
 
 // This is used in the Command Menu
 struct RestoreSubscriptionButtonView: View {
 // adapted from Apple's Backyard Bird's demo:
 @State private var isRestoring = false
 
 var body: some View {
 Button("Restore Subscription") {
 isRestoring = true
 Task.detached {
 defer { isRestoring = false }
 try await AppStore.sync()
 }
 }
 .disabled(isRestoring)
 }
 }
 
 #Preview {
 RestoreSubscriptionButtonView()
 }
 */
